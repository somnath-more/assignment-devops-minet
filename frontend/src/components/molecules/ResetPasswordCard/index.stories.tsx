import React from 'react';
import ResetPasswordComp from '.';

export default {
  title: 'Molecules/ResetPassword',
  component: ResetPasswordComp,
};

const Template = () => <ResetPasswordComp />;

export const ResetPasswordCard = Template.bind({});
